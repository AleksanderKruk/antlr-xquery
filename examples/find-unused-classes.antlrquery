(: find unused classes :)

let $classNames := //(classDeclaration|normalInterfaceDeclaration)
					/typeIdentifier ! text()
let $uses := //typeIdentifier ! text()
for $cn in $classNames
	let $usecount := $uses[$cn eq .] => count()
	return if ($usecount gt 1) { $cn }